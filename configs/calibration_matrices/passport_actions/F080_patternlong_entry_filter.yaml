version: 1
status: active_passport_action_matrix
created_utc: "2026-06-22T20:50:00Z"
source_of_truth: {action_passport_registry: configs/calibration_action_passports.yaml, action_passport: docs/CALIBRATION_NODE_CURRENT/passports/features/PATTERN_COMPOSITE_ENTRY_F080_F081_strict_passport.md, feature_formulas: src/mlbotnav/dataset.py, backtest_runtime: src/mlbotnav/backtest.py}
passport_mode:
  enabled: true
  registry_path: configs/calibration_action_passports.yaml
  registry_block_id: B024
  registry_passport_id: F080
  action_passport_id: F080
  action_id: F080_PATTERNLONG_ALLOW
  policy: allowlist
  allowed_calibration_params: [F080_pattern_family_filter, F080_direction_mode, F080_logic_mode, F080_use_strength, F080_use_age, F080_use_volume_confirm, F080_use_level_confirm, F080_structure_filter, F080_breakout_filter, F080_min_score, F080_block_opposite_pattern]
optuna_switches:
  enabled: true
  block_switches: {price_volatility: false, trend_momentum: false, volume_flow: false, density_profile: false, structure_ta: false, pattern: true}
  hypothesis_switches: {enabled: true, strict_block_purity: true}
  context_blocks: {enabled: false, always_on: [], calibration_policy: passport_action_only, comparison_policy: none}
grid_presets:
  wide: {description: "full passport profile value range"}
  medium: {description: "every second passport value with preserved max"}
  narrow: {description: "compact passport value set with preserved min/max anchors"}
parameter_profiles:
  F080_pattern_family_filter: {values: [1, 2, 3, 4], count: 4, axis: pattern_family_filter}
  F080_direction_mode: {values: [1, 2], count: 2, axis: direction_mode}
  F080_logic_mode: {values: [1, 2], count: 2, axis: logic_mode}
  F080_use_strength: {values: [0, 1], count: 2, axis: use_strength}
  F080_use_age: {values: [0, 1], count: 2, axis: use_age}
  F080_use_volume_confirm: {values: [0, 1], count: 2, axis: use_volume_confirm}
  F080_use_level_confirm: {values: [0, 1], count: 2, axis: use_level_confirm}
  F080_structure_filter: {values: [1, 2, 3, 4, 5], count: 5, axis: structure_filter}
  F080_breakout_filter: {values: [1, 2, 3, 4, 5], count: 5, axis: breakout_filter}
  F080_min_score: {min: 2, max: 7, step: 1, count: 6, axis: min_score}
  F080_block_opposite_pattern: {values: [0, 1], count: 2, axis: block_opposite_pattern}
feature_rows:
  - {block: pattern, feature: pattern_structure_volume_entry_long_1m, action_id: F080_PATTERNLONG_ALLOW, action_type: ENTRY_FILTER, calibrate: true, optuna_toggle: true, params: [F080_pattern_family_filter, F080_direction_mode, F080_logic_mode, F080_use_strength, F080_use_age, F080_use_volume_confirm, F080_use_level_confirm, F080_structure_filter, F080_breakout_filter, F080_min_score, F080_block_opposite_pattern]}
hypothesis_rows:
  - {hypothesis: none, calibrate: true, optuna_toggle: true, params: []}
search_grid_rows: []
constraints: []
backtest_subpassport: {exit_policy: tp_sl_timeout, timeout_exit_enabled: true, disable_timeout_exit: false, note: "Backtest exit controls are baseline runtime controls, not F080 knobs."}
