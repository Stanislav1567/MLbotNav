# ACTIVE LOCK: Единое ТЗ исполнения
Дата фиксации: 2026-05-23
Статус: ACTIVE (единый рабочий план)

## 1) Единственный источник плана
- Основной документ: `docs/TZ_TABLE_CANON_BYBIT_PARITY_2026-05-23_RU.md`
- Новые идеи/улучшения: только в backlog, без изменения порядка этапов P0->P5.

## 2) Порядок выполнения (без отклонений)
1. P0 Freeze и контракт.
2. P1 Канон свечей.
3. P2 Канон фич-таблицы.
4. P3 Канон сигналов/исполнения.
5. P4 Канон артефактов.
6. P5 Приемка.

## 3) Анти-раздувание
- Запрещено запускать новые большие контуры до закрытия текущего этапа.
- Запрещено менять структуру/логику этапов до прохождения gate текущего этапа.
- Дополнительные задачи фиксируются отдельно как `backlog`.

## 4) Анти-сбой / resume policy
- При обрыве/лимите: возобновление строго с последнего checkpoint.
- Перед продолжением проверяем:
  - текущий `active_step`;
  - что уже сделано (`done_items`);
  - что осталось (`next_actions`).

## 5) Текущий активный этап
- `active_step = P5` (финальная приемка контура после P4).

## 6) Done items
- P1 закрыт: `candles_canonical` + parity-report по дням.
- P2 закрыт: `feature_frame`/`feature_frame_full` + словари RU/EN + readable XLSX.
- P3 закрыт: `signal_frame` + `execution_trace` + `execution_trace_summary`, цепочка проверена gate-аудитом.
- P4 закрыт: per-run упаковка артефактов в `reports/runs/<run_id>/...` + `index.json` + `p4_audit_report.json`.

## 7) Next actions (строго по порядку)
1. Удерживать strict-gate после каждого фикса (`tz_gate_runner`).
2. Дальше идти в функциональные улучшения стратегии/гипотез только через этот gate.

## 8) Обязательное правило после каждого фикса (новый strict gate)
После каждого фикса и после каждого закрытого пункта ТЗ всегда выполняем:
1. Проверка текущего шага.
2. Полная проверка цепочки от начала активного ТЗ.
3. Сохранение отчета PASS/FAIL в `reports/qa_gate/`.

Команда (единая):
```powershell
$env:PYTHONPATH='src'
python -m mlbotnav.tz_gate_runner --step P4
```

Если шаг не `P3`, передаем нужный `--step` и при необходимости параметры окна:
```powershell
$env:PYTHONPATH='src'
python -m mlbotnav.tz_gate_runner `
  --step P2 `
  --symbol SOLUSDT `
  --timeframe 1m `
  --start-date 2026-05-20 `
  --end-date 2026-05-21
```
