# Статус По ТЗ На 2026-05-21

## Закрыто
1. Data Core:
   - Day ingestion UTC по Bybit.
   - Incremental ingestion + watermark + ingest runs.
   - DQ reports по `symbol+timeframe+day`.
2. ML Core:
   - Unified pipeline: features -> walk-forward -> backtest -> gate.
   - Leakage checks.
   - Search кандидатов по horizon/threshold.
3. Registry/Gates:
   - Candidate registry.
   - Champion promotion only on gate pass.
4. Monitoring:
   - Drift monitor (PSI) + alert artifact.
5. Packs:
   - Daily artifact packs with checksums + index.
6. CV Integrity (minimum):
   - Screenshot ingest + sha256 + perceptual hash + index.
7. Governance/Security minimum:
   - Audit log.
   - Retention and secret leak scan maintenance.
   - Rollback guard report.
8. SOP/Runbook:
   - Operational cycle and rollback steps documented.

## Ключевой факт
Исторически был получен `gate PASS` и успешный promote в champion для:
- `symbol=SOLUSDT`
- `timeframe=60m`
- отчет: `reports/pipeline/pipeline_report_SOLUSDT_60m_20260521T065130Z.json`
- champion: `models/registry/champion.json`
С 2026-05-21 активирован minute-first lock: текущая рабочая дорожка только `1m` до явного подтверждения этапа.

## Осталось в работе
1. Дожать `1m` до first PASS (сейчас fail по риску/шуму).
2. Углубить Security/Governance до полного enterprise-уровня (RBAC/KMS/retention policies на уровне окружения).
3. Расширить CV контур до полного Overlay/Annotation pipeline из ТЗ.

## Рабочий цикл (уже исполняется)
1. `ingest_incremental`
2. `pipeline_train_eval --promote-if-pass`
3. `stage_engine` (с автофиксацией `data/meta/stage_runtime_state.json`)
4. `monitor_drift`
5. `rollback_guard`
6. `build_pack`

## Process Lock (2026-05-21)
- Minute-first workflow is now enforced in code.
- `1m` is locked by default.
- 30-day runs require explicit user confirmation.
- Final minute-stage visual summary with entry point is mandatory before 30-day approval.

## Master Checklist Sync
- Canonical checklist: `docs/TZ_5PLUS_MASTER_CHECKLIST_RU.md`.
- This checklist is the execution source for 'finish spec first, then runs'.

## TA Step Added
- `prod_cycle` includes `technical_analysis` stage before train/eval.
- Config flag: `configs/prod_policy.yaml -> prod.cycle.run_technical_analysis`.

## TA Contour Status
- TA analytics now includes levels/patterns/signals/fallback_events in csv+parquet and xlsx snapshot export.
- Latest 30-day TA report: `reports/technical_analysis/ta_report_SOLUSDT_1m_20260521T093913Z.json`.

## New Progress (2026-05-21, later)
- Backtest logs enriched with `expected_move_pct`, `tp_reach_prob`, `sl_hit_prob`, `ev`.
- Packs index now exported as `packs/index.parquet` in addition to `packs/index.csv`.
- Stage criteria extended with no-trade/stability/daily-PnL checks toward section 19 AC.
- Champion promote now includes candidate-vs-champion guard (return delta, drawdown regression, sharpe delta).
- Rollback guard now supports executable rollback action (restore previous champion from registry history).
- SLA monitor added for `latency/error/perf` with `sla_report` and `sla_alert` artifacts.
- Pipeline M5 upgraded: `logreg/lightgbm/xgboost` model-family selection + feature-group ablation report.
- Stage ladder runtime added (`stage_ladder.py`) with D1 exhaustive search and stage-state persistence.
- Negative memory upgraded with reason tags and active-block summary; stage fingerprints now built from real run params/context.

## New Progress (P1 data-source expansion, 2026-05-21)
- Added market-context ingestion for Bybit v5:
  - funding: `src/mlbotnav/ingest_context_day.py`
  - open interest: `src/mlbotnav/ingest_context_day.py`
  - incremental day-batch: `src/mlbotnav/ingest_context_incremental.py`
- Added market-context contracts and DQ:
  - `src/mlbotnav/context_contract.py`
  - `src/mlbotnav/context_dq.py`
  - `data/dq/market_context_quality_report.csv`
- Added context records to meta/watermark flow:
  - `data/meta/ingest_run.csv`
  - `data/meta/ingest_pair_status.csv`
  - `data/meta/ingest_watermark.csv`
- Acceptance smoke done for `SOLUSDT` on `2026-05-20`; incremental report:
  - `reports/ingestion/context_incremental_SOLUSDT_20260521T134625Z.json`
- Bybit OI `1m` returns empty dataset in this contour; interval is explicitly marked as `skipped`, supported OI intervals are `5,15,30,60,240,D`.
