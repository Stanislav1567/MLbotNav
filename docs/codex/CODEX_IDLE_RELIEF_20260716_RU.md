# Codex idle relief 2026-07-16

Статус: `CODEX_IDLE_RELIEF_APPLIED_NO_DELETE_NO_STAS_TOUCH`.

## Граница безопасности

- Файлы и папки `STAS*`, включая `STAS5` и `STAS6`, не изменялись и не удалялись.
- Удалений не было.
- Codex Desktop не закрывался, чтобы не оборвать текущую сессию.
- Microsoft Defender (`MsMpEng`) не отключался и его настройки не менялись.

## Что сделано

1. Остановлен только один фоновый read-only процесс Git:
   - `git.exe -c core.hooksPath=NUL -c core.fsmonitor=false status --porcelain`;
   - позже остановлен еще один read-only внутренний `git diff --find-renames --numstat -z`;
   - после остановки `.git/index.lock` отсутствовал;
   - активных `git.exe` после контроля не осталось.
2. Приоритет процессов `Codex`, `codex` и `Code` понижен до `Idle`.
3. Остановлен отдельный VS Code OpenAI/Codex extension server:
   - `C:\Users\007\.vscode\extensions\openai.chatgpt-26.527.60818-win32-x64\bin\windows-x86_64\codex.exe app-server`;
   - это не удаление расширения, а остановка фонового процесса до следующего запуска/перезапуска VS Code.
4. Подтверждено, что локальные настройки VS Code для проекта уже снижают Git/Watcher-нагрузку:
   - `git.autorefresh=false`;
   - `git.autoRepositoryDetection=false`;
   - `git.scanRepositories=[]`.

## Замеры после разгрузки

Итоговый контроль:

- активных `git.exe` нет;
- `.git/index.lock` нет;
- процессы `Codex`/`codex`/`Code` стоят в `Idle`;
- свободная память оставалась примерно `13.3..13.8 GB`;
- по короткому PID-замеру сами процессы Codex не показали дисковых read/write за окно `5s`.

При этом в системных счетчиках были короткие всплески диска. Сырой процессный счетчик показал главный источник всплеска:

- `MsMpEng` - Microsoft Defender, до примерно `10..21 MB/s` read/write в коротком окне;
- Codex тоже появлялся в I/O-счетчике, но заметно ниже, примерно до `4..5 MB/s` в коротком окне;
- `explorer`, `Taskmgr`, `aida64`, Chrome и активный интерфейс Codex оставались заметными участниками CPU.

## Вывод

На момент проверки это не выглядит как зависший опасный Git-процесс или массовое чтение проекта Codex. Основной оставшийся дисковый всплеск идет от Microsoft Defender (`MsMpEng`). Codex/VS Code прижаты по приоритету и лишний VS Code extension server остановлен, но текущая открытая сессия Codex все равно будет потреблять CPU на renderer/GPU/app-server, пока окно активно.

## Что можно делать дальше только отдельным решением

1. Если после перезагрузки снова будет тяжелый старт, сразу снять процессные I/O-счетчики в первые 1-2 минуты и проверить, кто именно читает диск: Codex, Defender, VS Code или другой процесс.
2. Если подтвердится, что `MsMpEng` сканирует рабочую папку, можно отдельно обсудить безопасное исключение Defender для конкретных generated/run-папок. Это системная настройка безопасности, поэтому без прямого разрешения не менять.
3. Если VS Code снова поднимает OpenAI/Codex extension server и он не нужен, можно отдельно отключить расширение в VS Code. Сейчас расширение не удалялось.
